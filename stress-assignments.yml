config:
  target: "http://localhost:3001"
  phases:
    - duration: 60
      arrivalRate: 100 # 100 requests per second for 1 minute
scenarios:
  - flow:
      - get:
          url: "/api/assignments"
